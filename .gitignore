node_modules/
dist/
.env
.env.local
.next/
coverage/
*.log
# Git-Bash/MSYS crash dumps — diagnostic noise, dropped in the repo root
*.stackdump
# TypeScript incremental build caches — rewritten by every tsc/next build run
*.tsbuildinfo
apps/backend/.env
apps/backend/dist/
apps/frontend/.env.local
postgres_data/
redis_data/
